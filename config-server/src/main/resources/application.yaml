server:
  port: 7979
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/03AvilaJ/TurnsMicroservices
          search-paths: config-data
  application:
    name: config-server

